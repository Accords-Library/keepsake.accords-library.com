{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "db:push": "astro db push",
    "dev": "astro dev --remote",
    "build": "astro build --remote",
    "preview": "astro preview --remote",
    "check": "astro check --remote",
    "astro": "astro",
    "prettier": "NODE_OPTIONS=\"--experimental-strip-types\" prettier . --write",
    "lint": "astro check",
    "upgrade": "npx @astrojs/upgrade && ncu",
    "precommit": "npm run db:push && npm run prettier && npm run check && npm run lint && npm run build"
  },
  "dependencies": {
    "@astrojs/db": "^0.21.2",
    "@astrojs/node": "^10.1.1",
    "astro": "^6.3.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/upgrade": "^0.7.2",
    "npm-check-updates": "^22.2.0",
    "prettier": "3.8.3",
    "prettier-plugin-astro": "0.14.1",
    "typescript": "^5.9.3"
  }
}
