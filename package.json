{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "db:push": "astro db push",
    "dev": "astro dev --remote",
    "build": "astro build --remote",
    "preview": "astro preview --remote",
    "check": "astro check --remote",
    "tsc": "tsc",
    "astro": "astro",
    "prettier": "NODE_OPTIONS=\"--experimental-strip-types\" prettier . --write",
    "lint": "astro check",
    "upgrade": "npx @astrojs/upgrade && ncu",
    "precommit": "npm run db:push && npm run prettier && npm run tsc && npm run check && npm run lint && npm run build"
  },
  "dependencies": {
    "@astrojs/db": "^0.21.3",
    "@astrojs/node": "^11.1.5",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@astrojs/upgrade": "^0.7.4",
    "npm-check-updates": "^23.1.0",
    "prettier": "3.9.6",
    "prettier-plugin-astro": "1.0.0",
    "typescript": "^6"
  }
}
